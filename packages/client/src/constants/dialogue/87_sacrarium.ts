import { DialogueNode } from '.';

const blackpool: DialogueNode = {
  index: 871,
  text: [
    'This black ooze prevents you from descending further',
    'If it’s possible to open a way through, you’ll have to help me figure out how',
  ],
};

const pillars: DialogueNode = {
  index: 872,
  text: ['Ancient Greek in style and also age', 'Must be imported'],
};

export default [blackpool, pillars];
