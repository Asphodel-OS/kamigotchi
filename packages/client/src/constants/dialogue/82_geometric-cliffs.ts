import { DialogueNode } from '.';

const bluelantern: DialogueNode = {
  index: 821,
  text: [
    'You can’t pick this up',
    'I don’t have an inventory code for it',
    'Maybe it was important back in 1998',
  ],
};

export default [bluelantern];
